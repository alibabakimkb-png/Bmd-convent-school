services:
  - type: web
    name: bmd-convent-school
    runtime: python
    plan: free
    buildCommand: "echo ready"
    startCommand: "python server.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
